import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    borderTopColor: '#333333',
    borderTopWidth: 1,
  },
  textEmptyTask: {
    fontSize: 14,
    lineHeight: 20,
    textAlign: 'center',
    color: "#808080",
  },
})